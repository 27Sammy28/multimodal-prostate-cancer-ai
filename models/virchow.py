def load_virchow_model(model_name="paige-ai/Virchow"):
    raise NotImplementedError(
        "Virchow embedding extraction should be configured with the approved model "
        "weights and authentication for your environment."
    )
