fusion:
  architecture: transformer

pathology:
  embedding_dim: 2560

mri:
  embedding_dim: 1024

clinical:
  features:
    - psa
    - age
    - pirads
    - prostate_volume

transformer:
  hidden_dim: 512
  num_heads: 8
  num_layers: 4
  dropout: 0.1

output:
  classes: 6
